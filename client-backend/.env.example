# Database Configuration (for PostgreSQL container)
POSTGRES_USER=jambo_user
POSTGRES_PASSWORD=jambo123
POSTGRES_DB=savings_management

# Backend Database Configuration (for app container)
DB_NAME=savings_management
DB_USER=jambo_user
DB_PASSWORD=jambo123
DB_HOST=jambo-shared-db
DB_PORT=5432
NODE_ENV=development

# JWT Configuration
JWT_SECRET=secret-jambo-key
JWT_EXPIRES_IN=1h
ADMIN_JWT_SECRET=super-secret-jambo-key
ADMIN_JWT_EXPIRES_IN=8h

# Security
PASSWORD_SALT=password-salt-docker
CORS_ORIGIN=http://localhost:3000,http://localhost:3002,http://localhost:5173

# Database Configuration (for PostgreSQL container)
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Backend Database Configuration (for app container)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=
NODE_ENV=

# JWT Configuration
JWT_SECRET=
JWT_EXPIRES_IN=
ADMIN_JWT_SECRET=
ADMIN_JWT_EXPIRES_IN=

# Security
PASSWORD_SALT=
CORS_ORIGIN=
